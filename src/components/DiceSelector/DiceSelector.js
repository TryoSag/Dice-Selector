import { listOfDice } from "../../utils/utils";

const DiceSelector = () => {
  return (
    <>
      <ul>{listOfDice.map((dice)=>)}</ul>
      <button></button>
    </>
  );
};

export default DiceSelector;
